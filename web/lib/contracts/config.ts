import { inkSepolia } from "viem/chains";

// Default chain for contract interactions
export const DEFAULT_CHAIN = inkSepolia;

// Contract addresses -- fill these in as you deploy
export const CONTRACTS = {
  // Example:
  // xSPY: "0x..." as `0x${string}`,
  // vault: "0x..." as `0x${string}`,
} as const;

// ABIs -- add your contract ABIs here
// Example:
// export const xSPYAbi = [...] as const;
